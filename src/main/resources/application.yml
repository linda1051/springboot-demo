server:
  port: 8080
spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/linda?useUnicode=true&characterEncoding=utf-8
    username: root
    password: 123456
  mvc:
    view:
      suffix: .html
  resources:
    static-locations: classpath:/templates
  mybatis:
    mapper-locations: classpath:/mapper/*.xml